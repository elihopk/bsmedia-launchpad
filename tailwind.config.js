module.exports = {
  purge: [],
  darkMode: 'media', // or 'media' or 'class'
  theme: {
    extend: {},
  },
  variants: {
    extend: {
      padding: ['hover'],
      ringWidth: ['hover'],
      ringColor: ['hover'],
      ringOpacity: ['hover'],
    },
  },
  plugins: [],
}
